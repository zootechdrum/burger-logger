<li class="indi-burger text-center">
	{{burger_name}}
	<div>
	<button class="statusOfBurger btn" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if devoured}}Eaten{{else}}Devour It!{{/if}}
	</button>

	<button class="delete-burger btn" data-id="{{this.id}}">DELETE BURGER</button>
	</div>

</li>